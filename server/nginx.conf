http {
    include /etc/nginx/mime.types;

    upstream api {
        server api:$PORT;
    }

    server {
        listen 80;
        root /usr/share/nginx/html;

        location /api/ {
            proxy_pass http://api;
        }
    }
}

events {}
